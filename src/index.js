import React from "react";
import ReactDOM from "react-dom";

import ContainerRow from "./ConatinerRow";

const rootElement = document.getElementById("root");
ReactDOM.render(<ContainerRow />, rootElement);
